{% extends "home.html.twig" %}
{% block title %}
    Admin Page - Product
{% endblock %}
{% block body %}
<!--Main layout-->
<main>
<div class="container">
  <!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark mt-3 mb-5 shadow p-2" style="background-color: #607D8B">
  <!-- Container wrapper -->
  <div class="container-fluid" style="margin-top: 50px;">

    <!-- Navbar brand -->
    <a class="navbar-brand" href="{{path('homepage')}}">View:</a>

    <!-- Toggle button -->
    <button 
       class="navbar-toggler" 
       type="button" 
       data-bs-toggle="collapse" 
       data-bs-target="#navbarSupportedContent2">
      <i class="fas fa-bars"></i>
    </button>

    <!-- Collapsible wrapper -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent2">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <!-- Link -->
        <li class="nav-item acitve">
          <a class="nav-link text-black"  href="{{path('bookListShow')}}">Book</a>
        </li>
        <li class="nav-item acitve">
          <a class="nav-link text-black" href="{{path('show_author_list')}}">Author</a>
        </li>
        <li class="nav-item acitve">
          <a class="nav-link text-black" href="{{path('showborrowerlist')}}">List Borrower</a>
        </li>
          <li class="nav-item acitve">
          <a class="nav-link text-black" href="{{path('showbooktype')}}">Book Type</a>
        </li>
      </ul>
      
      <!-- Search -->
      <form class="w-auto py-1" style="max-width: 12rem" action="{{path('app_search')}}">
        <input type="search" class="form-control rounded-0" placeholder="Search" aria-label="Search" name="name">
      </form>

    </div>
  </div>

            
  <!-- Container wrapper -->
</nav>
<!-- Navbar -->
  <div className="page-heading pb-2 mt-4 mb-2 ">
                    {# <h3>Manager</h3> <a href="{{path('borrowerlist_create')}}"><button type="button" class="btn btn-outline-primary">Insert</button></a> #}
                </div>
                <div class="page-content">
                <div class="btn-group" role="group" aria-label="Basic outlined example">
               
                </div>
                <div class="card">
                    <div class="container mb-3">
                        <div class="table-responsive text-nowrap">
                      <table class="table table-striped">
                        <thead>
                          <tr>
                            <th>ID</th>
                            <th>User Name</th>
                            <th>Book Name</th>
                            <th>Borrowed Date</th>
                            <th>Return Date</th>
                            <th>Status</th>
                            {# <th>Quantity</th> #}
                            {# <th>Action</th> #}
                          </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                          {% for bl in borrowerlist %}
                              <tr>
                                  <td>{{ bl.id}}</td>
                                  <td>{{ bl.userbr}}</td>
                                  <td>{{ bl.book}}</td>
                                  <td>{{ bl.borrowedDate|date("m/d/Y")}}</td>
                                  <td>{{ bl.returnDate|date("m/d/Y")}}</td>
                                  <td>{{ bl.status}}</td>
                                
                              </tr>
                          {% endfor %}
                          
                        </tbody>
                      </table>
                    </div>
                </div>
                </div>
                </div>
{% endblock %}