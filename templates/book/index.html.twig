{% extends 'admin.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
  <div className="page-heading pb-2 mt-4 mb-2 ">
                    <h3>Manager</h3> <a href="{{path('book_create')}}"><button type="button" class="btn btn-outline-primary">Insert</button></a>
                </div>
                <div class="page-content">
                <div class="btn-group" role="group" aria-label="Basic outlined example">
               
                </div>
                <div class="card">
                    <div class="container mb-3">
                        <div class="table-responsive text-nowrap">
                      <table class="table table-striped">
                        <thead>
                          <tr>
                            <th>#</th>
                            <th>Name</th>
                            <th>Author</th>
                            <th>Discription</th>
                            {# <th>Quantity</th>
                            <th>Action</th> #}
                          </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                          {% for b in book %}
                              <tr>
                                  <td>{{ b.id}}</td>
                                  <td>{{ b.name}}</td>
                                  <td>{{ b.author}}</td>
                                  {# <td>{{ p.created|date("m/d/Y")}}</td> #}
                                  <td>{{ b.discription}}</td>
                                  <td>
                                    {# <a href="{{path('product_edit',{'id':b.id})}}">Edit</a>
                                  <a href="{{path('product_delete',{'id':b.id})}}">Delete</a></td> #}
                              </tr>
                          {% endfor %}
                          
                        </tbody>
                      </table>
                    </div>
                </div>
                </div>
                </div>
{% endblock %}
