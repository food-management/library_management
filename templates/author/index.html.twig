{% extends 'admin.html.twig' %}

{% block title %} Author Page !{% endblock %}

{% block body %}
  <div className="page-heading pb-2 mt-4 mb-2 ">
                    <h3 style="text-align: center;">Author Manager</h3> <a href="{{path('author_create')}}"><button type="button" class="btn btn-outline-primary">Insert</button></a>
                </div>
                <div class="page-content">
                <div class="btn-group" role="group" aria-label="Basic outlined example">
               
                </div>
                <div class="card">
                    <div class="container mb-3">
                        <div class="table-responsive text-nowrap">
                      <table class="table table-striped">
                        <thead>
                          <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Information</th>
                            {# <th>Author</th> #}
                            {# <th>Discription</th> #}
                            {# <th>Quantity</th> #}
                            <th>Action</th>
                          </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                          {% for a in author %}
                              <tr>
                                  <td>{{ a.id}}</td>
                                  <td>{{ a.name}}</td>
                                  <td>{{ a.information}}</td>
                                  {# <td>{{ a.author}}</td> #}
                                  {# <td>{{ p.created|date("m/d/Y")}}</td> #}
                                  {# <td>{{ a.discription}}</td> #}
                                  <td>
                                    <a href="{{path('author_edit',{'id':a.id})}}">Edit</a>
                                  <a href="{{path('author_delete',{'id':a.id})}}">Delete</a></td>
                              </tr>
                          {% endfor %}
                          
                        </tbody>
                      </table>
                    </div>
                </div>
                </div>
                </div>
{% endblock %}
