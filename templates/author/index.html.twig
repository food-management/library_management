<<<<<<< HEAD
{% extends 'base.html.twig' %}

{% block title %}Hello AuthorController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/library_management/src/Controller/AuthorController.php'|file_link(0) }}">src/Controller/AuthorController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'D:/library_management/templates/author/index.html.twig'|file_link(0) }}">templates/author/index.html.twig</a></code></li>
    </ul>
</div>
=======
{% extends 'admin.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}
  <div className="page-heading pb-2 mt-4 mb-2 ">
                    <h3>Manager</h3> <a href="{{path('author_create')}}"><button type="button" class="btn btn-outline-primary">Insert</button></a>
                </div>
                <div class="page-content">
                <div class="btn-group" role="group" aria-label="Basic outlined example">
               
                </div>
                <div class="card">
                    <div class="container mb-3">
                        <div class="table-responsive text-nowrap">
                      <table class="table table-striped">
                        <thead>
                          <tr>
                            <th>#</th>
                            <th>Name</th>
                            {# <th>Author</th> #}
                            {# <th>Discription</th> #}
                            {# <th>Quantity</th> #}
                            {# <th>Action</th> #}
                          </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                          {% for a in author %}
                              <tr>
                                  <td>{{ a.id}}</td>
                                  <td>{{ a.name}}</td>
                                  {# <td>{{ a.author}}</td> #}
                                  {# <td>{{ p.created|date("m/d/Y")}}</td> #}
                                  {# <td>{{ a.discription}}</td> #}
                                  <td>
                                    {# <a href="{{path('book_edit',{'id':b.id})}}">Edit</a>
                                  <a href="{{path('book_delete',{'id':b.id})}}">Delete</a></td> #}
                              </tr>
                          {% endfor %}
                          
                        </tbody>
                      </table>
                    </div>
                </div>
                </div>
                </div>
>>>>>>> 90e0760cb00980f78cd17fd8be7b4f351dff9850
{% endblock %}
